{
  "name": "end-to-end-api",
  "version": "0.0.1",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "4.14.x",
    "firebase": "^3.2.1",
    "fs": "0.0.2",
    "node-uuid": "^1.4.7",
    "request": "^2.72.0",
    "socket.io": "^1.4.8",
    "typescript": "^1.8.10",
    "util": "^0.10.3"
  },
  "scripts": {
    "postinstall": "npm install -g typings typescript tsc && typings install && npm run build",
    "watch": "tsc --p tsconfig.json --watch",
    "build": "tsc --p tsconfig.json",
    "start": "node dist/lib/main.js"
  },
  "typescript": {},
  "engines": {
    "node": "6.2.0"
  }
}
